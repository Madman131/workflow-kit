node_modules
.DS_Store
# if the kit is ever self-adopted:
.claude/task-lane.json
.claude/lane-ledger.jsonl
.claude/brief-rung.json

# Thread-restart digests — transient handoffs, stale within hours; commit one only on purpose.
thread-restarts/
