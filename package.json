{
  "name": "workflow-kit",
  "version": "1.1.0",
  "description": "Portable steer -> gate -> garden method + fail-closed enforcement controls for building production systems with AI agents.",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "bin": {
    "workflow-kit-init": "bin/init.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "acceptance": "bash acceptance/plant-the-bug.sh"
  }
}
