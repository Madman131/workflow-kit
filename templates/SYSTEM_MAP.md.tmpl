# SYSTEM MAP — {{REPO_NAME}} architecture snapshot

> **CLASS: BINDING** · **generated / regenerated per repo — an `as-of`-dated snapshot, never appended.**
> **as-of {{LAST_VERIFIED_DATE}}.** Cap: 8 KiB — keep it a bounded MAP, not the full architecture.
> A deeper reference doc (if any) is REFERENCE: look it up, do not read it whole.
>
> This is a boot-set doc: read whole at onboarding. It must fit in one read and stay current — an
> undated or stale snapshot is a trap (regenerate it on an architectural change). **Complete the
> placeholders.**

## What runs
{{WHAT_RUNS}}

## The pieces (map, not manual)
{{COMPONENTS}}

## Data / state
{{DATA_STATE}}

## Deploy & operate
- Deploy: pushing `{{DEPLOY_BRANCH}}` {{DEPLOY_EFFECT}}. {{DEPLOY_MECHANISM}}.
- Standing mechanical gate: {{MECHANICAL_GATE}}.

## Where to look deeper
{{DEEP_REFERENCES}}
